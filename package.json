{
  "name": "fairtasker",
  "version": "1.0.0",
  "description": "Fair gigs, zero rip-off platform fees. FairTasker: Taskers keep 100%, transparent pricing, Australian gig marketplace.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx postcss src/input.css -o src/output.css",
    "watch:css": "npx postcss src/input.css -o src/output.css --watch",
    "dev": "npx http-server . -p 3000 -o",
    "start": "npx http-server . -p 3000"
  },
  "keywords": [
    "gig economy",
    "local jobs",
    "fair trade",
    "no commission",
    "task marketplace",
    "Australia"
  ],
  "author": "FairTasker",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "firebase": "^10.14.1",
    "sonner": "^1.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.2.1"
  }
}

